export interface TransformedRace {
    id: string;
    name: string;
    slug: string;
    distanceMeters: string;
    startTime: string;
    eventName: string;
    status: string;
    statusId: string | number;
}